import { ReactElement } from "react";
import { PageStyled } from "../PageStyled";
import { ITestsPageProps } from "./types";

export function TestsPage(props: ITestsPageProps): ReactElement {
  return (
    <PageStyled>
      Settings
    </PageStyled>
  );
}